{% extends 'base.html.twig' %}

{# Override base body #}
{% block body %}
    <div class="container">
        <h1>{{ charity.name }}</h1>
        <hr>
        <p>{{ charity.description }}</p>
        <p>Address: {{ charity.address }}</p><br>
        <a href="/">Go Back</a>
    </div>
{% endblock %}