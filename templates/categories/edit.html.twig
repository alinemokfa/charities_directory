{% extends 'base.html.twig' %}

{# Override base title #}
{% block title %}Edit Category{% endblock %}

{# Override base body #}
{% block body %}
    <h1>Edit Category</h1>
    <hr>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}